[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-tile-fountain"
version = "0.1.0"
description = "Auto-generate tiles from docs, code comments, FAQ, ML outputs"
license = {text = "MIT"}
requires-python = ">=3.8"
